# Family tree — backlog

- **Adoption / single-parent children:** Today, children are only linked from a relationship junction (two parents). Add a way to attach a child to one person without a relationship—for example, a bottom handle on a person that creates a child edge, or an “adoption” link type—when this use case matters.
 